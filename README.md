# CI-CD-pipeline
Written to automate the build, test, and deployment process for a sample application in Node.js. It includes automated testing, code quality checks, and deployment to a staging environment.
